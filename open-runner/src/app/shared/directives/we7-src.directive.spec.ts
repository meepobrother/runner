import { We7SrcDirective } from './we7-src.directive';

describe('We7SrcDirective', () => {
  it('should create an instance', () => {
    const directive = new We7SrcDirective();
    expect(directive).toBeTruthy();
  });
});
